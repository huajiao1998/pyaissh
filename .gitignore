# Python
__pycache__/
*.pyc

# 测试证据区（不进版本库：避免噪音与敏感输出；stest_tmp 是本地审计工作区）
stest_tmp/

# 真实凭据文件（.env.example 是公开样例，不受此规则影响）
.env

# 零散测试输出
stest_r12_err1.txt
stest_r12_err2.txt

# 系统文件
.DS_Store
Thumbs.db

# DSH/Reasonix 会话元数据（非项目文件）
.reasonix/tasks/
.reasonix/desktop-topic-*.json

# 技能打包产物（构建/导出生成，非源码）
*.zip
.freebuff/
.stest/
reasonix.toml

ft_local_tmp/
# pyaissh-mcp 已入仓（自包含分发包：适配器 + pyaissh 完整副本 + 技能文档）；
# 测试只在本地保留，连同本地凭据一起忽略（双保险：目录内另有 .gitignore）
pyaissh-mcp/test/
pyaissh-mcp/.env
pyaissh-mcp/__pycache__/
tests/live/.env
tests/live/_tmp_*
