# pyaissh 域级 MANIFEST（第三版）：构建顺序 = 域文件顺序（显式，非 import 拓扑）
#
# 每个域文件 = 原单文件的连续大段（搬移期内容零改动，顺序保持原文件顺序）。
# 顺序尊重顶层执行语义（护栏 2）：
#   00 含极早期信号标志/注册（任何慢操作前）+ docstring + stdlib import
#   04 含 _setup_console_utf8() 与 _QUIET 的模块级执行点
#   01-11 常量/正则/函数定义按原顺序（模块级赋值依赖顺序）
#
# 域文件在 pyaissh-dev/domains/ 下：<序号>_<域名>.py
# 每行: <序号>_<域名>.py
#
# ⚠️ 方向是单向的（2026-09-16）：**域文件是源，成品单文件是产物**。
#   `build_single.py join` 按本文件行序拼接，并在每两个域之间插入
#   `# ===== [域 NN/12] <标题> =====` 横幅（横幅是 join 生成的，不在域文件里）。
#   曾经的 `split`（单文件 -> 域文件）已**移除**：它按"每域一个锚正则"切分，而锚是
#   域内标识而非域边界，本就无法还原域文件；旧实现还会先清空 domains/ 再切分，锚一过期
#   即"域文件被删空"。锚列随之删除（唯一消费者是 split）——要改代码就改 domains/ 后跑 dist。
#
# 本文件仍被 tests/run_tests.py 的 unit_artifacts 校验（12 域 + 域文件齐全）。
00_head.py
01_globals.py
02_cred_regex.py
03_env_paths.py
04_console_out.py
05_util.py
06_conn.py
07_sftp_transfer.py
08_cmd_exec.py
09_cmd_transfer.py
10_cmd_test_ls.py
11_cli_main.py
